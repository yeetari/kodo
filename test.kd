fn add(let a, let b) {
    return a + b;
}

fn main() {
    let foo = 10;
    return match ({
        let bar = foo + add(foo, 20);
        yield bar;
    }) {
        5 => 20,
        add(30, 10) => match (foo + 5) {
            15 => add(50, 60),
        },
    };
}
